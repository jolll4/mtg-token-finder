Oracle data pulled 20241229100204

```bash
python ./tokens.py -p "decklist.txt" -f withCount
```

Format options (WIP):

`withCount` (default)

```bash
1 Raise the Alarm
```

justName

```bash
Raise the Alarm
```
